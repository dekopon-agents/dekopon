/target/
/examples/providers/*/target/
.DS_Store
*.swp
*.swo
.env
.env.*
!.env.example
coverage/

# The end-to-end walkthrough tells a reader to copy the .example beside this path and paste a
# GitHub token into it. Ignoring it here means following the walkthrough cannot commit a secret.
/examples/pr-summarizer-linter/broker-credentials.yaml

# Code-review artifacts; system of record is the vagus vault, not git history.
/docs/review-2026-07/

# Container image build input, in case a release is ever extracted into the checkout rather than
# into the scratch directory ci/stage-image-context.sh stages. Never repository content; see
# docs/container-image.md.
/dist/
