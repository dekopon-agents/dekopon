# Workspace integration tests

Executable CLI integration tests live in [`crates/dekopon/tests`](../crates/dekopon/tests) so Cargo builds them with the `dekopon` package. This directory is reserved for future repository-level black-box fixtures and scripts.
